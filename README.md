аналог игры blockudoku. запустите main для запуска


```bash
git clone https://github.com/Dreksen/blockudoku.git
cd blockudoku
python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install -r requirements.txt
```
